Got it.
Your Jenkinsfile **uses `try/catch/finally`** — not `post` blocks.
So your POC job must follow the **same structure**, otherwise your pipeline will not execute correctly.

I will now give you a **clean, simple POC Jenkinsfile** that:

### ✅ Uses your same structure

* `node`
* `ansiColor`
* `try / catch / finally`
* Calls **updateGlobalCounter()**
* Supports **mock fortifyScanCount** for testing
* Has **SCENARIO** and **MOCK_COUNT** parameters
* Prints clear results for FAILURE / SUCCESS
* Does NOT depend on your real stages

---

# ⭐ FINAL — POC JENKINSFILE (MATCHES YOUR STRUCTURE)

> **Copy/paste this into a new Pipeline Job**
> Job name example: `fortify-notification-poc`

```groovy
properties([
    parameters([
        choice(
            name: 'SCENARIO',
            choices: ['FAILURE', 'SUCCESS', 'NORMAL'],
            description: 'Choose POC scenario'
        ),
        string(
            name: 'MOCK_COUNT',
            defaultValue: '0',
            description: 'Simulated fortifyScanCount for testing'
        )
    ])
])

node('any') {

    ansiColor('xterm') {

        try {

            println "============== FORTIFY POC TEST START =============="
            println "Selected Scenario       : ${params.SCENARIO}"
            println "Mock Fortify Count      : ${params.MOCK_COUNT}"
            println "==================================================="

            // Convert to integer
            def mockCount = params.MOCK_COUNT as Integer

            stage('POC: Run Scenario') {

                if (params.SCENARIO == 'FAILURE') {

                    println "POC → Simulating FAILURE scenario"
                    updateGlobalCounter("runCheck", mockCount)

                    // Fail the build on purpose
                    error("Simulated failure for POC testing")

                } else if (params.SCENARIO == 'SUCCESS') {

                    println "POC → Simulating SUCCESS scenario (reset notification)"
                    updateGlobalCounter("resetWithoutNotification")

                } else {

                    println "POC → Simulating NORMAL scenario (no threshold breach)"
                    updateGlobalCounter("runCheck", mockCount)
                }
            }

            println "POC Completed Successfully"

        } catch (err) {

            println "=========== FAILURE HANDLING BLOCK ==========="

            println "Build failed → Executing failure logic"
            def mockCount = params.MOCK_COUNT as Integer
            updateGlobalCounter("runCheck", mockCount)

            // Re-throw error so Jenkins marks build as failed
            throw err

        } finally {

            println "=========== FINALLY BLOCK EXECUTED ==========="
            println "Sending Final Teams Notification (if applicable)"

            sendTeamsNotification()

            println "============== POC TEST END =============="
        }
    }
}
```

---

# ⭐ HOW THIS WORKS WITH YOUR FAIL LOGIC

You use:

* `try {}` for pipeline stages
* `catch(err)` for failure handling
* `finally {}` for cleanup / notifications

So the POC job now matches this exactly.

---

# ⭐ WHAT THE POC JOB DOES

### 1️⃣ FAILURE Scenario

* Calls:

```groovy
updateGlobalCounter("runCheck", mockCount)
```

* Fails the build intentionally:

```groovy
error("Simulated failure")
```

* Goes into `catch`
* Calls `updateGlobalCounter()` again
* Sends Teams notification

### 2️⃣ SUCCESS Scenario

Calls only:

```groovy
updateGlobalCounter("resetWithoutNotification")
```

### 3️⃣ NORMAL Scenario

Calls:

```groovy
updateGlobalCounter("runCheck", mockCount)
```

---

# ⭐ Example Test Cases

| SCENARIO      | MOCK_COUNT | Expected                                      |
| ------------- | ---------- | --------------------------------------------- |
| FAILURE       | 20         | Sends ONE notification → sets `notified-true` |
| FAILURE again | 20         | No notification sent                          |
| SUCCESS       | 0          | Resets flag → `notified-false`                |
| FAILURE again | 20         | Sends ONE notification again                  |

---

# ⭐ Your Shared Library Requirements

Your updated function must look like this:

```groovy
def call(def countValReset, def mockCount = null) {
    ...
    def fortifyScanCount = (mockCount != null)
        ? mockCount
        : getFortifyPendingJobsCount(...)
}
```

---

If you want, I can also show you:

### ✔ How to integrate the POC logic into your real Jenkins pipeline

### ✔ How to debug the `notified-true / false` file

### ✔ How to simulate multiple failures in one run

Just say **"show integration example"**.
===========================================







### ✔️ YES — you can **safely comment out `sendTeamsNotification()`** in the POC job.

In fact, for Fortify notification testing, **you SHOULD disable it** because:

* It uses many fields (`buildStatus`, `env.failedStage`, user details)
* These values are **not initialized in the POC**
* So the function throws errors like:

  ```
  Cannot invoke method equalsIgnoreCase() on null object
  ```
* You ONLY need to test **Fortify one-time failure notification logic**, not Teams notification logic.

---

# ⭐ ✔️ What to Comment Out

In your POC Jenkinsfile, simply comment this line:

```groovy
// sendTeamsNotification()
```

Inside the finally block:

```groovy
finally {
    println "== FINALLY BLOCK EXECUTED =="

    // COMMENT OUT THIS FOR POC
    // sendTeamsNotification()

    println "========= POC TEST END ========="
}
```

---

# ⭐ Result After Commenting

### 😊 What works:

✔ `updateGlobalCounterFile("runCheck", mockCount)`
✔ `mockCount` successfully overrides `fortifyScanCount`
✔ ONE alert sent only for first failure
✔ No alert sent for repeated failures
✔ Alert sent again only after success reset

### ❌ What will not run (but we don’t care in POC):

* Teams notifications
* Status messages to product channels

---

# ⭐ For Your Test Objective

Your goal:

> **Send ONLY one notification when Fortify pending scan > threshold,
> then only send again after success reset.**

This test works perfectly **without** Teams notifications.

---

# ⭐ If you want complete verification steps:

### Test 1 — First Failure

Run:

* SCENARIO = FAILURE
* MOCK_COUNT = 20 (assuming threshold is 10)

Expected:

```
INFO: First failure → sending notification
INFO: notified-true
```

### Test 2 — Second Failure

Run same values again.

Expected:

```
INFO: Already notified earlier → skipping
```

### Test 3 — Success

Run:

* SCENARIO = SUCCESS
  Expected:

```
INFO: Resetting notification flag → notified-false
```

### Test 4 — Next Failure

Run:

* SCENARIO = FAILURE
* MOCK_COUNT = 20

Expected:

```
INFO: First failure → sending notification
```

---

# ⭐ If you want I can update your entire POC Jenkinsfile (clean, fixed, ready to paste).

