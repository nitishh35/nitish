// NOTE: No @Library annotation — we use the Jenkins shared lib namespace directly.
def repoName = REPOSITORY_NAME ?: "enterprise-paymybill-payment-methods-sync-up-testing"

// Load the shared library function
def lib = org.jenkinsci.plugins.workflow.cps.GlobalVariable.getBinding().getVariable("getDeployedLastVersion")

// Call the shared lib method
return lib.call(repoName)




second approach


// Safe sandboxed wrapper
return getDeployedLastVersion(REPOSITORY_NAME ?: "enterprise-paymybill-payment-methods-sync-up-testing")
